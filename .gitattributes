# Normalize line endings to LF in the repo so Windows dev and Linux CI agree
# (otherwise ruff format --check can disagree across platforms).
* text=auto eol=lf

*.png binary
*.jpg binary
*.parquet binary
*.ico binary
